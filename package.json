{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://sandrosaran.github.io/",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b main -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}